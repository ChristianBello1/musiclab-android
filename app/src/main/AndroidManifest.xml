<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permessi moderni per Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Permesso per notifiche su Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permesso legacy per Android 12 e precedenti -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permessi per controllare la riproduzione audio -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicLab">

        <!-- Servizio per notifica media -->
        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Activity principale -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity del player completo -->
        <activity
            android:name=".PlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.MusicLab"
            android:launchMode="singleTop" />

        <!-- Activity per vedere le canzoni nella cartella -->
        <activity
            android:name=".FolderSongsActivity"
            android:exported="false"
            android:theme="@style/Theme.MusicLab" />

        <!-- Activity per la gestione della coda di riproduzione -->
        <activity
            android:name=".QueueActivity"
            android:exported="false"
            android:theme="@style/Theme.MusicLab"
            android:launchMode="singleTop" />

        <!-- Activity per vedere le canzoni nella playlist -->
        <activity
            android:name=".PlaylistSongsActivity"
            android:exported="false"
            android:theme="@style/Theme.MusicLab"
            android:launchMode="singleTop" />

        <!-- Activity per le impostazioni -->
        <activity
            android:name=".SettingsActivity"
            android:label="Impostazioni"
            android:theme="@style/Theme.MusicLab"
            android:screenOrientation="fullSensor"
            android:exported="false" />

        <activity
            android:name=".HelpActivity"
            android:label="Guida"
            android:theme="@style/Theme.MusicLab"
            android:exported="false" />

    </application>

</manifest>